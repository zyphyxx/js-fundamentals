

let numOne = window.prompt('Digite um numero: ')
let numTwo = window.prompt('Digite o segundo numero:')

let result = parseInt(numOne) + parseInt(numTwo);
console.log(result);

window.alert(`O resultado foi: ${result}`);

window.confirm('Deseja realmente apagar?')
